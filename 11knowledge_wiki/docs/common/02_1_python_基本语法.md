

## Python的语言特色

1.python是一门解释性语言。

```
解释性语言	
	
	就是在执行过程中，必须自身带有解释器的语言，例如 PHP,python,java等
	
编译性语言
	
	系统可以直接识别的程序，不需要任何的解释工具，其语言的翻译过程在系统执行之前已经完成，；例如C,object-c

```

2.Python是一门弱类型语言

```
弱类型

	在系统保存数据时，不需要预先通知程序，保存数据的特征和格式，直接使用即可
	
强类型

	在系统保存数据是，需要将保存的数据大小和特征预先通知系统，系统准备合适的存储空间才能使用
```

3.python是一门面向对象的语言

```
	在python中，万物皆对象，开发过程中可以使用类和对象的各种特征进行程序开发。
```

## 检测python版本

1.打开运行,输入cmd或者commond ,输入python命令就可以进入交互模式(组后一行>>>)，交互模式的最开始信息中包含python版本信息。

```
C:\Users\Windows32>python
Python 2.7.14 (v2.7.14:84471935ed, Sep 16 2017, 20:19:30) [MSC v.1500 32 bit (Intel)] on win32
Type "help", "copyright", "credits" or "license" for more information.
>>>
```

2.打开运行，输入cmd命令，使用python -V 即可检测python版本信息

```
C:\Users\Windows32>python -V
Python 2.7.14
```

## python代码的分类

python代码主要分为两大类：单行代码和代码组/块。

单行代码

​	只有一行代码的的语句

代码组

```
指定结构的多行代码组成的一组内容
```

续行

```
如果单行内容写不开，可以将当前行的内容延续到下一行继续书写
在当前行的最后加一个\就可以在下一行继续书写当前单行代码的内容
```

## 程序输出

输出：

	print('需要输出的内容')

print语句调用str()显示对象，交互解释器调用repr()函数来显示对象。

下划线(_)在解释器中表示最后一个表达式的值。

与字符串操作符(%s)结合使用，可实现字符串替换功能。

```
>>> print "%s is Number %d" % ("Python",1)
Python is Number 1
>>>
```

- %s 字符串
- %d 整型
- %f 浮点型

## 程序输入

使用raw_input()内建函数，读取标准输入，并将读取到的数据赋值给指定的变量。

```
>>> user = raw_input('enter login name: ')
enter login name:
```

当输入一个数值字符串可以使用内置函数int()将字符串转换成整型值。

## 程序的注释

python的注释分为2种，一种是单行注释，用#号在前。
一种是多行注释，由两对三个单引号(')或三个双引号(")包含起来。

**单行注释**

```
# 需要注释的内容
```

**多行注释**

```
'''
第一行注释内容
第二行注释内容
...
第N行注释内容
'''
```

或者

```
"""
第一行注释内容
第二行注释内容
...
第N行注释内容
"""
```

## 操作符

标准算术操作符

```
+    加
-    减
*    乘
/    传统除法
//   浮点除法
%    取模运算，计算两个数相除之后的余数
**   乘方操作符
```

标准比较操作符

```
<
<=
>
>=
==
!=
```

逻辑操作符

```
and
or
not
```

## 常用命名方式

> 不管是文件，变量，函数或类名等等，命名都要遵守一个基本规范，就是，命名要有意义，易读易懂。因此，两种主流的命名方式出现了，他们分别是驼峰命名方法 和 使用下划线分隔的方式

### 驼峰命名

驼峰命名法还细分为两种：

```
1.小驼峰式命名法
    第一个单词首字母小写，之后的单词首字母大写，例如：userInfo、goodsList、articleList

2.大驼峰式命名法
    每个单词的首字母都使用大写字母，例如：UserCenter、DataInfo
```

### 下划线分隔命名

就是使用下划线_连接所有的单词，以便区分。如：

```
user_info   和   data_list
```

## 创建Python文件

**文件名称部分**

```
1.可以使用英文，不要使用中文
2.可以使用数字，但是尽量不要全用数字
3.可以使用_或者-等部分字符分隔文件名
4.文件命名要有意义方便维护
5.变量命名要避免和系统使用的保留关键字冲突
```

**文件后缀部分**

```
Python不限制文件后缀名，但建议使用更标准的常用格式
常用文件后缀：   .py    .pyw 
.pyw 格式是被设计来运行开发完成的纯图形界面程序的
但在开发纯图形界面程序的时候，你也可以暂时把 .pyw 改成 .py，方便运行时调出控制台的窗口，查看所有错误信息
```

